const { insurance } = require('./Insurance');
const { careProvider } = require('./CareProvider');
const { accessRequests } = require('./AccessRequests');
const { patientCase } = require('./PatientCase');

module.exports = {
  insurance,
  careProvider,
  accessRequests,
  patientCase,
};
